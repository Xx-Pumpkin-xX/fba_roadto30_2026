# DNSE client-segment prioritisation – Axis 2 (supply side)

Analysis for **FBA Season 6, Round 2** (DNSE case), Team B.

**Question:** in which client group (new investors, margin borrowers or derivatives traders) does DNSE still have the room and the capability to grow?

The notebook scores each group on four supply-side measures:
- **Momentum:** change in DNSE's market share, P0 (Q4/2024–Q2/2025) vs P1 (Q4/2025–Q2/2026).
- **Headroom:** growth still possible before a binding constraint.
- **Efficiency:** DNSE's rank against five listed peers.
- **Capability & price** (competition merged in): net position versus the market, scored 0 / 2.5 / 5 (features vs SSI, VPS, TCBS; price vs the peer median).

It benchmarks DNSE against SSI, VND, HCM, VCI and MBS, using financial statements, HNX derivatives brokerage shares and company disclosures.

## Pipeline

1. **Extract:** read only the hard-typed input cells from the team's Excel dataset.
2. **Clean:** fix date parsing, mixed definitions and text-in-number cells, logging every change.
3. **Validate:** re-compute every spreadsheet formula in pandas and compare with Excel (81/81 match), then cross-check key figures against official financial statements and DNSE annual reports.
4. **Metrics:** margin-ceiling utilisation and headroom, lending yield vs funding cost, brokerage contribution margin, share momentum.
5. **Score and test:** rule-based 1–5 scores and a sensitivity check (alternative definitions, time windows, dropping each measure).

## Figures

| | |
|---|---|
| ![Figure 8](figures/Fig08_B_market_share.png) | DSE share by client group |
| ![Figure 9](figures/Fig09_B1_margin_headroom.png) | Margin-ceiling utilisation and remaining headroom, Q2/2026 |
| ![Figure 10](figures/Fig10_B1_efficiency.png) | Net lending spread and brokerage contribution margin, H1/2026 |
| ![Figure 11](figures/Fig11_B_competition_price.png) | Price position of DNSE versus the peer median |

## Repository layout

```
notebook/B1_axis2_supply_side.ipynb   full analysis (runs with Restart & Run all)
figures/                              exported figures
data/clean/                           cleaned tables and outputs (regenerated by the notebook)
data/sources/                         figures read from official statements (quarterly codes 06/03/114), HNX share links, VSDC new accounts, fee schedules, capability matrix (Table 3)
data/raw/                             place the input dataset here (not included)
report/data_issues_log.md             tracker of every data issue, decision and status
requirements.txt
```

Key files in `data/clean/`:
- `Table4_axis2_scores_DRAFT.csv`: axis-2 scores.
- `Table4_axis2_evidence.csv`: the evidence behind each score.
- `Table_PL2_axis2_sensitivity.csv`: the sensitivity check.
- `L_bctc_ctck_quarterly.csv`: six brokers × Q3/2025–Q2/2026, codes 06 (brokerage revenue), 03 (interest on loans) and 114 (total loans), with sample totals.
- `B_dataset_long.csv` + `B_data_dictionary.csv`: the cleaned long-format dataset and its data dictionary.
- `B_cleaning_log.csv`: every fix applied to the data.
- `B_validation_vs_excel.csv`: the Excel re-computation check.
- `B_sources.csv`: the source register.
- `B_total_loans_consistent.csv`: total loans (margin + advances, code 114) for 6 brokers × 4 dates, verified against the statements.
- `2.4_capability_price_scored.csv`, `2.4_derivatives_fee_3cols.csv`: capability & price scoring and the derivatives fee split (broker / exchange / VSDC).

## How to run

```bash
pip install -r requirements.txt
# put "FBAR2_TEAM B DATASET.xlsx" in data/raw/  (see data/raw/README.md)
jupyter nbconvert --to notebook --execute --inplace notebook/B1_axis2_supply_side.ipynb
```

The notebook rewrites every file in `data/clean/` and `figures/` on each run.

## Notes

- Money is in VND billion. Ratios are decimals. Share changes are in percentage points.
- The input dataset is competition material, so it is not published.
